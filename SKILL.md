---
name: jma-disaster-pre-scale
version: 1.0.0
description: 気象庁防災情報XMLを監視し、既存アプリケーションを改修せずにAWSインフラを事前スケールアウトするための設計・実装・検証・運用支援Skill。
license: Proprietary
---

# JMA Disaster Pre-Scale Skill

## 目的

平時の利用が少なく、災害発生時に数万IDが一斉アクセスするシステムに対し、気象庁防災情報XMLを外部監視し、アクセス集中前にAWSリソースを段階的に拡張する。

最重要条件は、既存アプリケーションのソースコード、DBスキーマ、業務ロジックへ変更を加えないことである。

## 適用範囲

このSkillは次の作業を支援する。

1. 現行インフラ構成と変更可能範囲の確認
2. 気象庁XMLの監視方式設計
3. 災害種別・対象地域・警戒レベルの判定ルール設計
4. ECS、EC2、EKS、Aurora、RDS等の事前拡張設計
5. CloudFront、AWS WAF、ALB、仮想待合室による前段保護
6. Step Functions、Lambda、EventBridgeによる自動制御
7. 重複排除、訂正、解除、失敗時のフェイルセーフ
8. 負荷試験、復旧試験、運用手順、監査証跡の作成

## 最重要制約

以下を厳守する。

- 既存アプリケーションのコードを変更しない。
- 既存DBのテーブル定義や業務データ形式を変更しない。
- 既存システムの外側に監視・判定・制御基盤を追加する。
- インフラ設定の変更可否は事前に確認する。
- XML取得失敗時は縮小せず、現在容量を維持する。
- 解除情報を受信しても即時縮小せず、クールダウン時間を設ける。
- 地震等の突発災害では、事前スケールだけに依存せず常時予備容量を持つ。
- 本番変更はDry Run、承認、段階適用、ロールバックを可能にする。

## 入力情報

作業開始時に次を確認する。

- AWSアカウント、リージョン、対象環境
- 現行構成: ECS / EC2 / EKS / オンプレミス
- ロードバランサー、CloudFront、WAFの有無
- DB種類、最大接続数、リードレプリカ構成
- 認証基盤、セッション管理方式
- 起動時間、ウォームアップ時間、ヘルスチェック時間
- 想定登録ID数、同時アクセス率、ピークRPS
- 対象地域、災害種別、情報レベル
- 平時容量、警戒容量、最大容量
- 自動縮小までの待機時間
- 変更可能なインフラ範囲
- 手動承認を必要とする操作

不足情報がある場合は、合理的な仮定を明示し、Dry Run設計を優先する。

## 標準アーキテクチャ

```text
EventBridge Scheduler
        |
        v
JMA XML Poller Lambda
        |
        +--> XML取得・検証
        +--> 新着判定・重複排除
        +--> 対象地域・災害レベル判定
        |
        v
Step Functions
        |
        +--> Dry Run / Approval
        +--> ECS・EC2・EKS拡張
        +--> Aurora・RDS容量確認
        +--> CloudFront・WAF制御
        +--> ヘルスチェック
        +--> 通知・監査ログ
        |
        v
Existing System
```

## 実行フロー

### Phase 1: 現行評価

1. 既存システムが水平スケール可能か確認する。
2. ステートレス性、セッション、共有ファイル、DB接続を確認する。
3. Web層以外のボトルネックを特定する。
4. 既存URLをCloudFrontやALB配下へ置けるか確認する。
5. インフラAPIを操作するIAM権限範囲を確定する。

### Phase 2: XML監視

1. Atomフィードを定期取得する。
2. HTTPタイムアウト、再試行、User-Agentを設定する。
3. XMLサイズ上限、Content-Type、スキーマ、必須項目を検証する。
4. 電文ID、発表時刻、情報種別をキーに重複排除する。
5. 訂正、取消、解除、続報を識別する。

### Phase 3: 判定

次の組合せでスケールレベルを決定する。

```text
災害種別 + 対象地域 + 情報レベル + 現在容量 + クールダウン状態
```

標準レベル:

- LEVEL_0: 変更なし
- LEVEL_1: 最小予備容量へ拡張
- LEVEL_2: 通常時の数倍へ拡張
- LEVEL_3: 最大想定容量へ拡張
- HOLD: 現在容量維持
- SCALE_IN: 段階的縮小

### Phase 4: 事前スケール

対象環境に応じて以下を実施する。

- ECS/Fargate: Desired Count、Min Capacityを引き上げる。
- ECS/EC2: Auto Scaling Groupを先に拡張し、その後タスク数を増やす。
- EC2: Auto Scaling GroupのMin/Desired Capacityを変更する。
- EKS: Managed Node GroupとDeployment Replica数を増やす。
- Aurora Serverless v2: 最小ACUを一時的に引き上げる。
- Aurora Provisioned: リーダー追加または事前起動済みリーダーを利用する。
- RDS: 即時インスタンス変更に依存せず、事前の最大接続・IOPS余力を確保する。

### Phase 5: 前段保護

既存システム無改修を維持し、必要に応じて次を追加する。

- CloudFrontによる静的コンテンツキャッシュ
- AWS WAFレートベースルール
- AWS Shield Standard
- ALBヘルスチェック
- 仮想待合室
- カスタムエラーページ

### Phase 6: 検証

次を試験する。

- XML正常受信
- XML重複受信
- XML取得失敗
- 訂正・取消・解除
- 対象外地域
- Dry Run
- スケールAPI失敗
- 一部リソースだけ成功
- ヘルスチェック失敗
- 数万ID相当の負荷試験
- 段階縮小
- 手動強制拡張
- 自動制御停止

## 出力成果物

依頼に応じて次を作成する。

- 現行構成評価書
- 事前スケール設計書
- AWS構成図
- 判定ルール表
- IAMポリシー案
- Lambda実装
- Step Functions定義
- TerraformまたはCloudFormation
- 負荷試験計画
- 運用手順書
- 障害対応手順書
- 監査ログ仕様

## 安全設計

- 本番API呼出し前にDry Runを提供する。
- 最大容量に上限を設ける。
- 同一イベントの多重実行を防ぐ。
- DynamoDB等で分散ロックを行う。
- 操作前後の容量を記録する。
- CloudTrailとCloudWatch Logsへ証跡を残す。
- SNS、Slack等へ成功・失敗を通知する。
- 失敗時は縮小せず、現在容量を維持する。
- 手動オーバーライドを最優先する。

## 禁止事項

- 未検証XMLをそのままインフラ操作へ接続しない。
- XML受信だけで即座に無制限スケールしない。
- 地震時にゼロ台からの起動だけに依存しない。
- DB、認証、外部APIの限界を無視してWeb層だけ増やさない。
- 解除受信直後に平時容量へ戻さない。
- 本番環境で負荷試験を無承認で実施しない。
- 既存アプリケーションを無断変更しない。

## 応答形式

設計回答は原則として次の順で提示する。

1. 実現可否
2. 前提条件
3. 推奨アーキテクチャ
4. 既存システム無改修の実現方法
5. 判定ルール
6. 実装手順
7. テスト計画
8. リスクと対策
9. 運用方法
10. 次に作成すべき成果物
