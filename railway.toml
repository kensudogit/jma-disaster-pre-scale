[build]
builder = "RAILPACK"

[deploy]
startCommand = "PYTHONPATH=/app/src python -m uvicorn jma_pre_scale.web:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
